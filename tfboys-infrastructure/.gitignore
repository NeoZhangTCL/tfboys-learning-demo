# Terraform state files and backups
*.tfstate
*.tfstate.*

# Terraform variable files containing sensitive data
*.tfvars
envs/*.tfvars
environments/**/*.tfvars

# Terraform plans
*.tfplan

# Terraform cache and plugins (but keep lock file)
.terraform/
# .terraform.lock.hcl should be committed for version consistency
crash.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Lambda deployment packages
*.zip
modules/lambda/*.zip

# Private keys and certificates - 重要：防止私钥被提交到版本控制
keys/*.pem
keys/*.key
*.pem
*.key

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log

# Temporary files
*.tmp
*.temp

# Environment files
.env
.env.local
.env.*.local

# Archive files that shouldn't be committed
*.tar.gz
*.tgz
*.zip
*.rar

# Backup files
*.backup
*~