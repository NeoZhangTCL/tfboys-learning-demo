# Infrastructure-specific gitignore rules
# Global rules are handled in the root .gitignore

# Terraform variable files containing sensitive data
*.tfvars
envs/*.tfvars
environments/**/*.tfvars

# Terraform plans and specific cache files
*.tfplan
*.tfplan.*
.terraform.lock.hcl.backup
crash.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Lambda deployment packages
*.zip
modules/lambda/*.zip
modules/lambda/users-lambda.zip
modules/lambda/products-lambda.zip

# Private keys and certificates
keys/*.pem
keys/*.key
*.pem
*.key

# Terraform-specific logs
terraform.log

# Terraform debugging and configuration
.terraform.d/
.terraformrc
terraform.rc

# Local development and testing (infrastructure-specific)
test-results/

# AWS CLI and credentials (should never be committed)
.aws/
aws-credentials
credentials