# Backend-specific gitignore rules  
# Global rules are handled in the root .gitignore

# Go build artifacts and binaries
bootstrap
services/*/bootstrap
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go test and coverage files
*.test
*.out
*.prof
coverage.txt
*.cover

# Go mod cache and vendor
vendor/
go.sum.backup

# Debug and profiling (Go-specific)
debug
*.pprof

# Docker files (backend-specific)
.dockerignore
docker-compose.override.yml

# Backend-specific logs directory
logs/